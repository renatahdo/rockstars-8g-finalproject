import { FC } from 'react';
import { InputAdornment, Button, Box, TextField, Typography } from '@mui/material';
import SearchIcon from '@mui/icons-material/Search';
import { styles } from './CoverStyles'

export const Cover: FC = () => {
    return (
        <Box sx={ styles.cover }>
        
        </Box>
    )
}